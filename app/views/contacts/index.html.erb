<h1>Contacts</h1>
<%= link_to 'Add new', '/contacts/new' %>
<ul>
  <% @contacts.each do |contact| %>
    <li>
      <a href="/contacts/details?id=<%= contact.id %>"><%= contact.name %></a>
    </li>
  <% end %>

</ul>

